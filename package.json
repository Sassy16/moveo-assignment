{
  "name": "moveo-assignment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-client": "cd client && npm ci || npm install",
    "install-server": "cd server && npm ci || npm install",
    "build": "npm run install-client && npm run build --prefix client",
    "start": "npm run install-server && npm start --prefix server",
    "postinstall": "npm run build"
  }
}
